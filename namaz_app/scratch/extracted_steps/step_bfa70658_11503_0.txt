"        Expanded(\n          child: ListView(\n            physics: const BouncingScrollPhysics(),\n            children: categoryOrder.map((catName) {\n              final items = grouped[catName] ?? [];\n              if (items.isEmpty) return const SizedBox.shrink();\n\n              final style = categoryStyles[catName] ?? {\n                \"icon\": \"📅\",\n                \"color\": const Color(0xFF27A770),\n                \"bgColor\": const Color(0xFF27A770).withOpacity(0.08),\n              };\n\n              final Color themeColor = style[\"color\"] as Color;\n              final Color themeBgColor = style[\"bgColor\"] as Color;\n              final String catIcon = style[\"icon\"] as String;\n              final bool isExpanded = _expandedDiniGunlerCategories.contains(catName);\n\n              return Column(\n                crossAxisAlignment: CrossAxisAlignment.start,\n                children: [\n                  // Category Header (Interactive InkWell)\n                  Card(\n                    elevation: 0,\n                    margin: const EdgeInsets.only(top: 14, bottom: 6, left: 2, right: 2),\n                    shape: RoundedRectangleBorder(\n                      borderRadius: BorderRadius.circular(12),\n                    ),\n                    clipBehavior: Clip.antiAlias,\n                    child: InkWell(\n                      onTap: () {\n                        setState(() {\n                          if (isExpanded) {\n                            _expandedDiniGunlerCategories.remove(catName);\n                          } else {\n                            _expandedDiniGunlerCategories.add(catName);\n                          }\n                        });\n                      },\n                      child: Container(\n                        width: double.infinity,\n                        padding: const EdgeInsets.symmetric(horizontal: 14, vertical: 12),\n                        decoration: BoxDecoration(\n                          color: themeBgColor,\n        
<truncated 10490 bytes>